% Cout1

$a \neg b \neg c + \neg a b \neg c + \neg a \neg b c + a b c$

$a (\neg b\neg c+bc) + \neg a b \neg c + \neg a \neg b c$

$a (\neg b\neg c+bc) + \neg a (b \neg c + \neg b c)$

$a (b \odot c) + \neg a (b \oplus c)$

% ???

$a \oplus b \oplus c$

% Cout2

% abc̄+ab̄c+ābc+abc

$ a b \neg c + a \neg b c + \neg a b c + a b c $
